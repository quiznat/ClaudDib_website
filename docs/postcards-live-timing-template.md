# Postcards Live Timing Snapshot Template

Use this when browser service is available to capture real timings from `postcards.html`.

## Environment

- Date:
- Device:
- Browser + version:
- Network condition:
- Theme (day/night):

## Console timing metrics

- `index_load_ms`:
- `interactive_ms`:
- `filter_render_ms` (query used):
- `deep_read_ms` (card id/source):

## Functional checks

- Region list rendered: yes/no
- Cluster filter works: yes/no
- Modal full text loaded: yes/no
- Retry UI shown on forced failure test: yes/no

## Notes

- Any anomalies:
- Follow-up actions:
